apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  - ../base

resources:
 - certificate.yaml
#  - authentication.yaml

patches:
  - deployment-custom.yaml
#  - ingress-custom.yaml

#patchesJson6902:
#  - target:
#      version: v1
#      kind: Service
#      name: db-service
#      namespace: employees-$CI_ENVIRONMENT_SLUG
#    path: rds-svc-patch.yaml